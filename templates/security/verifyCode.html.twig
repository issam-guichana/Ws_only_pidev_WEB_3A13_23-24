{% extends 'base2.html.twig' %}


{% block header %}{% endblock %}

{% block sidebar %}{% endblock %}

{% block footer %}{% endblock %}
{% block body %}

	<div class="container">
		<section class="section register ">
			{% for message  in app.flashes('message') %}
				<div class="alert alert-info">
					{{ message }}
				</div>
			{% endfor %}
		</section>
		<section class="section register ">
			{% for danger  in app.flashes('danger') %}
				<div class="alert alert-danger">
					{{ danger }}
				</div>
			{% endfor %}
		</section>
		<section class="section register min-vh-100 d-flex flex-column align-items-center justify-content-center py-4">
			<div class="container">
				<div class="row justify-content-center">
					<div class="col-lg-4 col-md-6 d-flex flex-column align-items-center justify-content-center">

						<div class="d-flex justify-content-center py-4">
							<a href="" class="logo d-flex align-items-center w-auto">
								<img
								src="Uploads\logo-formini-final.png" alt="">
							{# <span class="d-none d-lg-block">Formini.tn</span> #}
							</a>
						</div>
						<!-- End Logo -->

						<div class="card mb-3">

							<div class="card-body">

								<div class="pt-4 pb-2">
									<h5 class="card-title text-center pb-0 fs-4">Mot de passe oublié</h5>
									<p class="text-center small">Enter your personal phone number</p>
								</div>

								<form method="post">
									{{ form_start(verificationForm) }}
									<div class="col-12">
										{{ form_row(verificationForm.verificationCode, {'attr': {'class': 'form-control', 'placeholder': 'Enter verification code'}}) }}
									</div>
                                    <br>
									<button type="submit" class="btn btn-primary w-100">Verify Code</button>
									{{ form_end(verificationForm) }}
								</form>

							</div>
						</div>
					</div>
				</div>
			</div>

		</section>

	</div>

{% endblock %}
