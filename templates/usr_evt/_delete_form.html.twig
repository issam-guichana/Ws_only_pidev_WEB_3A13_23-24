<form method="post" action="{{ path('app_usr_evt_delete', {'idUE': usr_evt.idUE}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ usr_evt.idUE) }}">
    <button class="btn">Delete</button>
</form>
