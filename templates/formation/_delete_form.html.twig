<form method="post" action="{{ path('app_formation_delete', {'idForm': formation.idForm}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formation.idForm) }}">
	<button class="btn">Supprimer</button>
</form>
