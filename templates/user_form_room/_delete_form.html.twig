<form method="post" action="{{ path('app_user_form_room_delete', {'idFR': user_form_room.idFR}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_form_room.idFR) }}">
    <button class="btn">Delete</button>
</form>
