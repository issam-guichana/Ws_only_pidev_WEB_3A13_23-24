<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Quiz List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        h1 {
            color: #333;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        .quiz-image {
            max-width: 150px;
            max-height: 100px;
        }
    </style>
</head>
<body>
    <h1>Liste des Quizzes</h1>
    <table>
        <thead>
            <tr>
                <th>Nom du Quiz</th>
                <th>Image</th>
            </tr>
        </thead>
        <tbody>
            {% for quiz in quizzes %}
                <tr>
                    <td>{{ quiz.nomQuiz }}</td>
                    <td>
                           <img src="{{ asset(quiz.image) }}" alt="Quiz Image" style="width: 100px; height: auto;"/>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
